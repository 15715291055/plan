# 本次优化说明

更新时间：2026-09-22

本次更新围绕排程、每日容量、任务完成状态和人工计划块进行了完整修复。

## 排程与计划块

- 自动排程默认使用每天 `08:00-23:00` 的学习窗口，避免无可用时间规则时生成午夜计划。
- 自动排程会扣除已经存在的未来计划块，避免同一时间被重复占用。
- 旧版本遗留的午夜自动计划块会在重新排程或加载时清理，并重新安排到正常时间窗口。
- 人工调整的计划块支持修改到任意日期和任意时间。
- 人工、导入、锁定和已完成的计划块在重新排程时保留，不会被自动排程覆盖。
- 计划块保存后记录人工来源和调整时间，后续排程可以识别其来源。

## 每日容量

- 每日容量成为独立设置页签。
- 容量滑块增加输入校验、保存状态和刷新后的持久化读取。
- 数据库结构或读取失败时不再静默回退为默认容量。
- 支持 0% 容量、单日覆盖和特殊模式继承。
- 固定课程、不可用时间、未来计划和跨午夜时段统一参与容量计算。

## 任务与习惯

- 已完成任务从默认活动任务列表隐藏。
- 完成任务时清理该任务的未来计划块，并提供恢复 RPC。
- 记录人工调整事件。
- 至少积累 3 次样本后才使用统计结果形成排程偏好，避免单次操作造成误判。

## 数据与部署

- 本地存储和 Supabase 的计划数据都支持新的来源与锁定语义。
- Supabase schema 和 migration 已包含容量、人工排程和习惯学习所需结构。
- API、类型检查、lint、测试和生产构建均已验证通过。

## 验证结果

```text
npm run typecheck 通过
npm run lint       通过
npm test           31 项通过
npm run build      通过
git diff --check   通过
```

生产部署：<https://plan-dhfo3nrkb-ray-01cd.vercel.app>

最新代码提交：`f1fe945 fix: replace stale overnight automatic schedules`
